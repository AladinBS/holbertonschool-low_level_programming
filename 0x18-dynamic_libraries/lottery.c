#include "holberton.h"
#include <stdio.h>
#include <string.h>

/**
 * lottery - check the code for Holberton School students.
 * @argc: string
 * @argv: string
 * Return: Always 0.
 */
int lottery(int argc, char *argv[])
{
if (strcmp(argv[1], "9") && strcmp(argv[2], "8") && strcmp(argv[3], "10") && strcmp(argv[4], "24") && strcmp(argv[5], "75") && strcmp(argv[6], "9"))
{
printf("--> Please make me win!");
}
return(0);
}
